#include <stdio.h>

int main() {
    int decimalNumber;

    printf("Enter a decimal number: ");
    scanf("%d", &decimalNumber);

    printf("hexadecimal equivalent: %x\n", decimalNumber);

    return 0;
}
